"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const stream_1 = require("stream");
class StreamSpeechImpl {
    stream;
    constructor() {
        this.stream = new stream_1.Stream();
    }
    close() {
        this.stream.removeAllListeners();
    }
    on(event, callback) {
        if (event === "transcript") {
            this.stream.on("data", (data) => {
                callback(data);
            });
        }
        if (event === "error") {
            this.stream.on("error", (error) => {
                callback(error);
            });
        }
    }
    emit(data) {
        this.stream.emit("data", data);
    }
}
exports.default = StreamSpeechImpl;
