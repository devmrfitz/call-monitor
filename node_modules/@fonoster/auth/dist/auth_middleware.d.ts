import { Context } from "./types";
export default class AuthMiddleware {
    privateKey: string;
    whitelist: string[];
    constructor(privateKey: string, whitelist?: any[]);
    middleware: (ctx: Context, next: () => Promise<void>, errorCb: (err: any) => void) => Promise<void>;
}
