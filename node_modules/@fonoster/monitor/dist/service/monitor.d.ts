import grpc, { ServerWritableStream } from "@grpc/grpc-js";
import { Event, SearchEventsRequest } from "./protos/monitor_pb";
import { IMonitorServer, IMonitorService, MonitorService } from "./protos/monitor_grpc_pb";
declare class MonitorServer implements IMonitorServer {
    [name: string]: grpc.UntypedHandleCall;
    searchEvents(call: ServerWritableStream<SearchEventsRequest, Event>): Promise<void>;
}
export { MonitorServer as default, IMonitorService, MonitorService };
