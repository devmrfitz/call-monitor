export interface ClientOptions {
    endpoint?: string;
    bucket?: string;
    accessKeyId?: string;
    accessKeySecret?: string;
}
export interface WebClientOptions {
    endpoint?: string;
    bucket?: string;
    accessKeyId: string;
    accessKeySecret: string;
}
