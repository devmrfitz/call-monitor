import { SGatherOptions } from "./types";
import { Verb } from "../verb";
import { SpeechProvider } from "@fonoster/common";
export default function startSpeechSource(sessionId: string, options: SGatherOptions, verb: Verb, speechProvider: SpeechProvider): Promise<{
    speechStream: import("@fonoster/common").StreamSpeechResult;
    token: string;
}>;
