import { GatherOptions } from "./types";
import { SpeechProvider } from "@fonoster/common";
import { Verb } from "../verb";
declare const waitForSpeech: (sessionId: string, options: GatherOptions, verb: Verb, speechProvider: SpeechProvider) => Promise<string>;
export default waitForSpeech;
